[project]
name = "us-relocation-planner"
version = "0.1.0"
description = "미국 이주를 준비하는 한국인 부부를 위한 체크리스트/플래너 앱"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "streamlit>=1.40.0",
]

[project.scripts]
planner = "main:main"
